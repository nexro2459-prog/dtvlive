// Auto-imported from maruf-tv.blogspot.com + telezo.lovable.app source playlists.
// Regenerated: prioritizes BD/Bangla playlists then international IPTV-org lists.
export interface Channel {
  id: string;
  name: string;
  category: string;
  logo: string;
  streams: string[];
  embedSlug?: string;
  type?: "hls" | "iframe";
}

export const channels: Channel[] = [
];

export const categories = ["All"];
